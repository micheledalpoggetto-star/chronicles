# Sprint 1 — Foundations

## Stato
Implementato il primo livello data-driven. Il prototipo usa ora un Campaign object versione 3, pur mantenendo alias compatibili con la UI esistente.

## API
- `ChroniclesCampaign.baseCampaign()`
- `ChroniclesCampaign.createLegacyCompatible()`
- `ChroniclesCampaign.migrate()`
- `ChroniclesCampaign.addHistory()`
- `ChroniclesSave.save()` / `load()` / `exportFile()`
- `ChroniclesUniverses.list()` / `get()` / `register()`
